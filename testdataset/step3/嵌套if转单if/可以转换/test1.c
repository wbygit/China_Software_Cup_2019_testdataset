/// for if to if  and  can transform
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int a=0;
    int b=0;
    int c=0;
    int d=0;
    if(a==0 && b==0)
    {
        if(b==0)
        {
            if(c==0)
            {
                if(d==0)
                {
                    printf("Transform\n");
                }
            }
        }
    }
}